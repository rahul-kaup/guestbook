# lombok config

# configure lombok to stop looking for further lombok config files
config.stopBubbling=true

# configure lombok to add @Generated annotation to generated classes so they are ignored during code coverage analysis
lombok.addLombokGeneratedAnnotation=true
